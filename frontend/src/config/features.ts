/**
 * Funciones en desarrollo que todavía no deben mostrarse a los usuarios.
 *
 * Cambia el valor a `true` únicamente cuando la experiencia esté lista
 * para volver a publicarse.
 */
export const ENABLE_PLAYLIST_COMPARISON = false;
